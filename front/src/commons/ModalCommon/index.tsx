import React, { useEffect, useRef, useState } from "react";
import { ModalBackgroundStyled, ModalRootContainerStyled } from "./styled";

interface IProps {

}

const ModalCommon: React.FC<IProps> = () => {

  return (
        <ModalBackgroundStyled >

        </ModalBackgroundStyled>
  );
};

export default ModalCommon;
